interface:
  display_name: "Resolve Dependabot PRs"
  short_description: "Consolidate safe Dependabot updates"
  default_prompt: "Use $resolve-dependabot-prs to consolidate, verify, merge, and close every safe Dependabot update in the current repository."

policy:
  allow_implicit_invocation: true
