# Frontend Architecture

Before modifying this frontend, read
`../docs/internal/architecture/frontend-architecture.md`.

That file is the source of truth for structure, import boundaries, SDD/TDD,
HTTP/BFF rules, React Query usage and Definition of Done. Prefer it over any
older layer-based frontend guidance.
