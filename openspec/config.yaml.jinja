schema: spec-driven

context: |
  [[ project_name ]] is a multi-tenant starter for authenticated B2B products:
  a FastAPI backend (Clean Architecture + DDD, async SQLAlchemy, modules
  under backend/src), a Next.js frontend (feature-first, app -> features ->
  entities -> shared, BFF routes under /api), and a React Router 7 +
  Fumadocs docs site in docs/. See AGENTS.md for detailed conventions and
  working rules.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
